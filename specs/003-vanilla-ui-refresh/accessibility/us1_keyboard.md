# US1 Keyboard Accessibility

- Verified that list item buttons remain keyboard focusable in the populated macOS browser QA run.
- Verified visible focus uses the shared `:focus-visible` outline and selected rows retain contrast.
- Verified populated row action menus can still be dismissed with `Escape` and outside click.
- Linux Chromium spot-check confirmed populated workspace parity; native Linux desktop keyboard and screen-reader verification remains a follow-up item.
