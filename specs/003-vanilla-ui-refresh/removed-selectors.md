# Removed Selectors

- `list-row`

Notes:

- `list-row` was defined in `frontend/src/app.css` but is not referenced by current Svelte markup.
- Additional selector cleanup should only happen after the remaining manual QA tasks are complete.
